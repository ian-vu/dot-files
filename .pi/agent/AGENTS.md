# Tool checklist

1. `rm` if trying to use this tool. You should use `trash` instead. It is safer and ensures anything deleted will go to macOS trash. This means `rm -rf` recusive and folder use should use as well.

# Writing style

- Always replace em dashes `—` with hyphens: use `-`, never the em dash character.
